const root = process.env.REACT_APP_API_ROOT_ENDPOINT;

export const ENDPOINT_SUBMIT = `${root}/apply`;
export const ENDPOINT_LOGIN = `${root}/login`;
export const ENDPOINT_INTERVIEW = `${root}/interview`;
